from __future__ import divisionimport nltk#nltk.download()from nltk.book import *import dateutilimport pyparsingimport numpyimport siximport matplotlibfrom nltk.corpus import PlaintextCorpusReaderimport oscurrent_directory = os.path.dirname(__file__)corpus_root = os.path.abspath(current_directory)wordlists = PlaintextCorpusReader(corpus_root, 'Islip13Rain/.*')# The next step is to show the file names under the directory (optional step)wordlists.fileids()ClassEvent =  nltk.Text(wordlists.words())# text7 datafdist7 = FreqDist(text7)V = set(text7)list_of_s_words = sorted([w for w in V if len(w) > 7 and fdist7[w] > 55])for w in list_of_s_words:	print w, fdist7[w]print text7.collocations()# ClassEvent datafdist_event = FreqDist(ClassEvent)vocab1 = fdist_event.keys()Vocab = set(ClassEvent)# Converts titlecase words to lowercase wordslist_of_single_words = [w.lower() for w in Vocab if len(w) > 6 and fdist_event[w] > 16]# Gets rid of duplicate wordslist_of_single_words = sorted(list(set(list_of_single_words)))print list_of_single_wordsprint ClassEvent.collocations(num=11)